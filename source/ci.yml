Image:
    type : wm-fe-centos6u5 
BeforeBuild:
    script :
Build:
    script : 
AfterBuild:
    script : ./build.sh
Package:
    script :
    artifacts:
        name : h5
        version : $COMMIT_ID-$BUILD_ID
        files : [./output] # seperate multiple files by comma
